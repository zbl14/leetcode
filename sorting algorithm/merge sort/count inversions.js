// Inversion Count for an array indicates - how far or close the array is
// from besing sorted. If array is already sorted then inversion count is 0.
// If array is sorted in reverse order that inversion count is the maximum.

// Formally speaking, two elements a[i] and a[j] form an inversion if
// a[i] > a[j] and i < j

// input const nums = [8, 4, 2, 1]
// Out put: 6
// Explanation: Given array has 6 inversions:
// (8,4), (4,2), (8,2), (8,1), (4,1), (2,1)
